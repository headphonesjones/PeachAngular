'use strict';

describe('myApp.scheduleDetail module', function() {

  beforeEach(module('myApp.schedule'));

  describe('scheduleDetail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SchedCtrl = $controller('SchedCtrl');
      expect(SchedCtrl).toBeDefined();
    }));

  });
});